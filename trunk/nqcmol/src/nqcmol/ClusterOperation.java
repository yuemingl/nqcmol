/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package nqcmol;
import java.util.Random;

/**
 *
 * @author chinh
 */
public class ClusterOperation {
    Random ranMov = new Random();

    double[][] param;

    public void RandomAtomicMove(Cluster mol){
//        double[] vec=new double[3];
//        for(int i=0;i<mol.getNAtoms();i++)
//				if(ranMov.nextDouble()< param[t].p[1]){
//					MTools.generateRandomVector(vec, ranMov, vec,param[t].p[2]);
//					mol.Translate_atom(i,vec,1);
//				}
    }
}
